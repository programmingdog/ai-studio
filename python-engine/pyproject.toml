[project]
name = "aivs-python-engine"
version = "0.1.0"
description = "AI Video Studio local JSONL worker"
requires-python = ">=3.8"
dependencies = ["PyMuPDF>=1.24,<2", "websocket-client>=1.8,<2"]

[tool.pytest.ini_options]
testpaths = ["tests"]
