- 自动工作流视频并发改由服务端推荐配置，配置为 `0` 时不限制并发。
- 支持供应商 Key 池按当前运行任务数选择，并固定任务创建与查询所使用的 Key。
- 自动压缩高分辨率参考图，单图控制在 10MB 以下、总请求控制在 48MB 以下。
- 自动工作流启动后锁定已确认的积分报价，避免执行中途因后台改价而停止。
- 支持在图片、视频区域一键重启失败任务，并完整显示任务错误信息。
- 登录有效期调整为七天，并支持临近过期自动刷新。
- 修复积分套餐赠送积分与套餐售价的展示逻辑。
